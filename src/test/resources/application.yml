spring: #스프링 부트에서 기본적으로 아무런 설정이 없으면 알아서 메모리모드로 돌린다.
#  datasource:
    #url: jdbc:h2:tcp://localhost/~/jpashop;  #MVCC=TRUE
#    url: jdbc:h2:mem:test;  #메모리모드
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver

#  jpa:
#    hibernate:
#      ddl-auto: create-drop  #완전히 다 끝난 후 자원정리까지 해준다.
#    properties:
#      hibernate:
##        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type : trace #쿼리 실행 파라미터를 로그로 남긴다.